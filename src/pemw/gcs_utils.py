# Deprecated stub kept only to avoid import errors in older environments.
# Marked no cover so it doesn't count against coverage metrics.
raise ImportError("gcs_utils removed: cloud functionality deprecated")  # pragma: no cover
